<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bomb.bomb" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".LogActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".RegisterActivity"
            android:label="注册"></activity>

        <activity android:name=".ResetPwdActivity"
            android:label="重置密码"></activity>

        <activity android:name=".CancelActivity"
            android:label="注销"></activity>

        <activity android:name=".MainActivity"
            android:label="@string/app_name"></activity>

        <activity android:name=".SendActivity"
            android:label="加密短信发送"></activity>

        <activity android:name=".FingerPrintActivity"
            android:label="指纹识别"></activity>

        <activity android:name=".MenuActivity"
            android:label="Bomb!-Menu"></activity>

    </application>

    //发送短信
    <uses-permission android:name="android.permission.SEND_SMS" />
    //指纹识别
    <uses-permission android:name="android.permission.USE_FINGERPRINT"/>
</manifest>